# JS 深浅拷贝

深浅拷贝只存在于引用数据类型中

## 什么是浅拷贝/深拷贝

以一个数组`a`为例，作为引用类型在栈中`a`对应的值是一个指向一块堆内容中的地址。如果仅做`let b = a`,此时实质上是把 a 的地址传入了 b 的栈的值，不论是单独对`a`还是对`b`的操作都会直接影响另一个变量的内容————这种就叫**浅拷贝**

为解决这个问题，首先就要给`b`开拓一块新的地址，再把`a`数组内容拷贝传入到`b`内存中————**深拷贝**:独立，且结构相同
