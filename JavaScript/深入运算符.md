# 深入运算符

## 逻辑运算符

主要包括 与`&&` , 或 `||`,非 `!`

- 返回值是逻辑运算中比较有趣的点
    - && : 拿 a && b举例, 当a布尔值为真时, 返回b。a布尔值为假时返回a(这里与逻辑判断短路机制有关，如果a值为假后边值不会执行)
    - || : 类似上边, 同样与短路机制有关, a真返回a, a假返回b
    - ! : 作为单目运算符是逻辑运算符中唯一一个一定返回布尔值的， a假返回true, 真返回false