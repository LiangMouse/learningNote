# CSS 预编译语言

CSS 预编译语言是用于扩展和增强 CSS 功能的编程语言，通过引入变量、嵌套规则、函数、继承机制等特性，使 CSS 开发更加高效、结构化和可维护。

## 常见 CSS 预编译语言

1. Sass: 支持两种写法：SCSS：语法与 CSS 兼容，支持大括号和分号。Sass：缩进式语法，不使用大括号和分号。

## 作用

CSS 预编译语言的作用主要有以下几点：

### 引入变量
