# Webpack 和 Vite 对比

Vite 使用浏览器对 ESM 的支持，无需打包，直接进行按需加载模块，启动速度快，热更新也非常高效
Webpack 在开发时需要打包生成 bundle 文件，但不需要浏览器的 ESM 支持特性，对老版本的兼容性更好

#### 热更新上的不同

什么叫做热更新？

指在开发过程中，能够在不重新加载整个页面的情况下，动态地替换、更新已修改的模块。这样开发者可以在修改代码后立即看到效果，而不需要刷新整个页面，显著提高了开发效率。

Vite 提供开箱即用的热更新功能，而 Webpack 需要额外的插件支持。
